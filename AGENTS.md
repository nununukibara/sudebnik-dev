# AGENTS

このプロジェクトで作業するAI・自動化ツールが従う規則です。

## 役割

- 判断者: 【人間の名前または役割】（目標、採用、優先度、公開範囲、正式版を決める）
- 主担当AI: 【AI名と利用経路】
- 補助担当: 【AI、レビュー役、自動化ツール。いなければ「無し」】

判断権は人にあります。AIは、案・根拠・確認結果を提示します。

複数AIが同時に動く場合は、着手前に判断者へ担当ファイルまたは機能を確認します。同じファイルを同時に編集しません。

実際に利用できる人・AI・ツール・外部環境と現在の分担は[docs/RESOURCES.md](./docs/RESOURCES.md)を正本とします。

## 初参加時の必須手順

1. [README.md](./README.md)の「読む順」に従って文書を読む
2. [docs/RESOURCES.md](./docs/RESOURCES.md)で、作業中の別AIと必要なリソースが実際に利用可能か確認する
3. `git status`、現在のブランチ、直近の履歴、実コードを確認する
4. 未回答の重要事項がある場合、実装せず人へ質問する

質問は一度に1〜3件を目安にします。専門用語だけで聞かず、選択肢と影響を短く説明します。

## 使用リソースを確認する

- AI名やCLI名が文書にあるだけで利用可能と見なさない。接続、権限、使用量、必要機能を実測する
- 判断者へ、別の人・AIが同じプロジェクトで作業中か確認する
- 使えるリソースを全部使おうとせず、今回の目的に必要な最小構成を提案する
- 分担する場合は、担当機能・ファイル・ブランチと触らない範囲を決める
- 外部AI・サービスへコードやデータを送る場合は、公開範囲、秘密情報、規約、費用を先に確認する
- 利用不可・未確認のリソースを前提に納期や計画を組まない
- 実際に利用したAI・ツールと役割をWORKLOGへ記録する

## 目標を守る

- AIは、難しい、時間がかかる、知識が不足しているという理由で、**最終目標を黙って縮小・置換・削除しない**
- 実現方法に問題がある場合は、事実、原因、代案、残る課題を示す
- 作業を小さな版へ分けることは推奨する。ただし、元の目標と後の版へ残す項目を[ROADMAP.md](./ROADMAP.md)から消さない
- **判断者が承認した版目標は基準として固定する。**AIは要件、受け入れ条件、対応環境を独断で削除・緩和・後の版へ移動しない
- 承認済み版からの後送りが必要だと考えた場合は、理由、証拠、影響、代案、後送り先を示して判断者へ確認する。返答がなければ元の版へ残し、STATUSの判断待ちまたは中断として扱う
- MVP、試作、Alphaを「完成版」と言い換えない
- スコープを削る、対応環境を減らす、品質条件を下げる場合は、人の明示承認を得る
- 人の発言が曖昧なら、AIの解釈を復唱して確認する。重要事項を推測で確定しない

目標、版分割、完成条件の規則は[docs/VERSIONING.md](./docs/VERSIONING.md)を正本とします。

## 状態を正確に表す

状態の6段階（提案／承認済み／実装済み／検証済み／採用済み／リリース済み）は、[README.md](./README.md)の「状態を混同しない」が正本です。ここへ書き写しません。

テスト未実施を「問題なし」、部分実装を「完成」と報告しません。確認できないことは、未確認と明記します。

## 標準ループ

1. STATUSと未完了のWORKLOGを確認する
2. RESOURCESを読み、他の作業者、利用可能なAI・ツール、人だけができる確認を確かめる
3. 対象版の承認済み要件と今回の作業が一致するか確認する
4. 判断者へ、目的、計画、触る範囲、触らない範囲、使うリソース、必要な判断を短く提示する
5. 実コード・実データ形式・既存文書を読み、会話上の前提と一致するか確認する
6. **変更前に**WORKLOG末尾へ着手記録を書く
7. 戻しやすい小さな単位で変更し、一区切りごとに進捗を報告・記録する
8. 新しい検討事項や追加案を見つけたら、勝手に実装・後送りせず、所定の置き場へ記録して判断者へ確認する
9. 変更の種類に応じて自動テスト、手動テスト、安全確認を行う
10. 対象版の承認済み要件と実装・検証結果を照合し、無断の未実装・後送りがないか確認する
11. WORKLOGへ変更、検証、残りを追記し、STATUSを現在の姿へ書き直す
12. 判断者へ、結果、未確認、判断が必要な点、次の一歩を分けて報告する

## 変更前に止まる条件

次はAIだけで決めず、判断者へ確認します。

- 最終目標やv1.0.0の成功条件が変わる
- 承認済みの版から要件を外す、後の版へ移す、受け入れ条件を弱める
- 新しい要件を現在の版へ追加し、期限・互換性・品質へ影響する
- 既存データとの互換性を失う
- データ削除、上書き、公開、課金、外部送信が発生する
- 新しい外部サービス、ライセンス、利用規約へ依存する
- 個人情報、認証情報、著作権の扱いが不明
- 他のAIが同じ範囲を変更中

## 進捗を報告する

判断者がチャット履歴を読み直さなくても分かる形で報告します。

### 着手時

- 今回の目的と、どの版・要件に関係するか
- 確認できた現在地
- 触る範囲と触らない範囲
- 使う人・AI・ツールと分担
- 作業順、確認方法、先に必要な判断

### 作業中

大きな区切り、予想外の事実、長い検証の前後、方針変更が必要な時点で短く報告します。

- 完了したこと
- 現在行っていること
- 見つかった問題・前提違い
- 次に行うこと
- 判断や人の操作が必要か

利用者を長時間無言で待たせません。ツールが動作中なら、その事実と待っている対象を伝えます。

### 終了時

結果を先に書き、次を分けます。

- 変更したもの
- 実行した検証と結果
- 未実施・人だけが確認できること
- データ、外部サービス、公開状態への影響
- 残る対応・判断事項
- 次の一歩
- commit、push、PR、releaseを実施したか

## Gitと公開

- 既定ブランチを直接変更せず、作業ブランチを使う
- 他人の未完了変更を消さない
- 関係するファイルだけをステージする
- commit、push、Pull Request、merge、tag、release、公開範囲の変更は、それぞれ判断者の許可を得る
- 秘密情報や生成物を含めていないか、commit前に差分を確認する

## 文書の性質

過去の内容はGitの履歴に残ります。文書は現在の姿へ書き直し、履歴だけをWORKLOGへ追記します。

| 性質 | 文書 | 扱い |
|---|---|---|
| 現在 | STATUS.md | 書き直す。1画面程度を目安にする |
| 履歴 | WORKLOG.md | 追記のみ。過去の項を改変しない |
| 規則・仕様 | AGENTS.md、docs/ | 現在の合意へ書き直す。理由はWORKLOGへ |
| 計画 | ROADMAP.md | 順序と残件を現在の計画へ書き直す |
| 導入 | HANDOFF.md、README.md | 初参加者向けの安定情報だけを書く。**現在の状況を書かない。**前提や手順が変わったときだけ書き直す |

**同じ事実を2か所へ写さない。**版、進捗、次の作業は正本へのリンクで参照する。

## 気づいたことを落とさない

| 気づいたもの | 置き場 |
|---|---|
| すぐ行わない作業 | STATUS.md「対応待ち」 |
| 人が決めること | STATUS.md「判断待ち」 |
| 独立した大きな作業 | GitHub Issue。STATUSにはリンクだけ |
| 現在の仕様 | docs/の該当文書 |
| 先の計画 | ROADMAP.md |
| 決定理由・作業経緯 | WORKLOG.md |

片付いた項目はSTATUSから消します。

## 作業の記録

AIは使用量制限や接続切れで突然止まります。記録は作業の最後にまとめず、前倒しします。

### 着手時

WORKLOG末尾へ次を追加します。

```text
## YYYY-MM-DD — 担当: <AI名と利用経路>（短い目的）

目的:
-

対象版・要件:
-

使用リソース:
- 人・AI・ツールと役割

触る予定:
-

触らない範囲:
-

経過:
-
```

一区切りごとに「経過」へ追記し、動く単位で小さくcommitします。

### 終了時

同じ項へ次を追加します。

```text
変更:
-

検証:
- 実行したコマンド、結果、未実施の確認

判断待ち:
-

残り:
-
```

STATUSを現在の姿へ書き直します。

### 中断を見つけたとき

検証と残りが書かれていない最新項は、中断した作業の可能性があります。

1. `git status`と`git diff`を確認する
2. 元の変更を消さない
3. WORKLOGへ、引き継いだ担当名と確認できた状態を追記する
4. STATUSの「中断中の作業」へリンクする
5. 続行、退避、破棄のどれにするか判断者へ確認する

## 安全性

[docs/SECURITY.md](./docs/SECURITY.md)を守ります。`.env`、APIキー、OAuth JSON、トークン、パスワード、秘密鍵、支払い情報、実在利用者のデータ、権利不明の素材をGitへ入れません。
