# AGENTS

このプロジェクトで作業するAIが従う規則。

## 役割

- 判断: 【人間の名前】（採用・優先度・公開範囲の判断）
- 主担当: 【担当AIの名前】（【経路。例: Notion AI／チャット経由】）
- 実装: 【実装するAI・ツール】
- 複数エージェントが同時に動く場合は、着手前に判断者へ分担（担当ファイル・機能単位）を確認してから着手する。競合を避けるため、同じファイルを同時に触らない

## 標準ループ

1. 短い計画
2. 実装前に現実チェック（実コード・既存文書との整合）
3. **`WORKLOG.md` へ着手の見出しを書く**（下記「作業の記録」）
4. 変更を実施、動作確認
5. WORKLOGへ検証結果、`STATUS.md` を書き直す
6. 判断者が採用判断

## 原則

- 正本はrepo。チャットの会話に判断や仕様を残さない
- 薄く保つ。戻しやすい粒度で進める
- 迷ったら止めて報告

## 文書の5つの性質（これを間違えると文書が腐る）

過去はgitが持っている。だから**文書は現在の姿だけを書き、古くなったら書き直す**。書き足して残すのはWORKLOGだけ。

| 性質 | 文書 | 扱い |
|---|---|---|
| **現在** | [STATUS.md](./STATUS.md) | **書き直す。**古い記述は消す |
| **履歴** | [WORKLOG.md](./WORKLOG.md) | **追記のみ。**過去の項を書き換えない |
| **規則・仕様** | AGENTS.md、`docs/` | 変わったとき**書き直す**。決めた理由はWORKLOGへ |
| **計画** | [ROADMAP.md](./ROADMAP.md) | 済んだら「済」にして本文は削る |
| **導入** | [HANDOFF.md](./HANDOFF.md)、README.md | 初めて入る人向け。**現在の状況を書かない** |

**同じ事実を2か所へ写さない。**版の数字、進捗、次にやること——写した瞬間から片方が嘘になる。参照先へのリンクだけを置く。

## 気づいたことを落とさない

**その場で対処しないと決めたことは、会話ではなくファイルへ置く。**置き場は1か所だけ。

| 気づいたもの | 置き場 |
|---|---|
| やるべき作業（すぐやらない） | `STATUS.md`「対応待ち」へ1行 |
| 人が決めること | `STATUS.md`「判断待ち」へ1行 |
| 大きい・独立して着手できる | Issue。STATUSからはリンクだけ |
| 仕様・設計の決定 | `docs/`の該当文書。決めた理由はWORKLOG |
| 先の計画 | `ROADMAP.md` |

**片付いたら消す。**STATUSは書き直す文書なので、済んだ行を残さない。

## 作業の記録（中断に耐える形）

**AIは使用量の上限で予告なく止まる。止まった瞬間には何も書けない。**だから記録は前倒しで書き、復旧は次の担当が行う。

### 着手するとき

`WORKLOG.md` の**末尾へ**見出しを作る。**終わってからではなく、始める前に。**既存の記録は古い順なので、新しい項を先頭へ差し込まない。

```
## YYYY-MM-DD — 担当: <実体名>（短い目的）

目的:
-

触る予定:
-

経過:
-
```

担当名は経路まで書く（例: `○○（△△経由）`）。ツール名だけでは誰か分からない。

### 進めている間

**一区切りつくたびに「経過」へ1行足す。**「何をどこまでやったか」だけでよい。ここが中断したときの唯一の手掛かりになる。溜めない。

コミットも記録になる。**動く状態になったらこまめにコミットする。**

### 終えるとき

同じ項へ追記する。

```
変更:
-
検証:
- 実行したコマンドと結果
残り:
-
```

そのうえで `STATUS.md` を**書き直す**。追記ではない。

### 中断していたとき（次に入る者の義務）

**検証が書かれていない項は、中断した作業である。**止めた本人は書けないので、**見つけた者が引き継ぐ。**

1. `git status` と `git diff` を見る。作りかけの変更がそこに残っている
2. WORKLOGのその項へ、**引き継いだ者の名前で**「中断時点でここまで」と書き足す。**元の記述は消さない**
3. `STATUS.md` の「中断中の作業」へ、WORKLOGの該当項へのリンクと状態を書く
4. 続けるか捨てるかは判断者に確認する。**勝手に消さない**

## 複数AIでの引き継ぎ

- 作業開始時に [STATUS.md](./STATUS.md) を読む。初めてこのrepoに入るなら先に [HANDOFF.md](./HANDOFF.md)
- 複数が同時に動くときは着手前に分担を確認する。**同じファイルを同時に触らない**
- 他AIの未完了変更を上書きしない。担当範囲が不明なら編集前に止めて報告する
